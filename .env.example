# Copie para .env e ajuste se necessário.
# Postgres via Docker (docker compose up): porta 5433 no host → 5432 no container.
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/lead_to_pack?schema=public"
